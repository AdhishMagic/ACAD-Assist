AWS Deployment Guide (Preview)

Sections:
1) Prerequisites (AWS account, CLI, IAM user)
2) Build + containerize app
3) Push image to ECR
4) Deploy to ECS Fargate
5) Logging + monitoring (CloudWatch)

HOD Notes:
- Must include least-privilege IAM policies.
- Avoid hard-coded secrets; use SSM/Secrets Manager.
