services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: acad_assist
      POSTGRES_USER: acad_user
      POSTGRES_PASSWORD: acad_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U acad_user -d acad_assist"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DJANGO_SECRET_KEY: change-me-to-a-strong-64-char-secret-key-for-production-use-2026
      DJANGO_DEBUG: "False"
      DJANGO_ALLOWED_HOSTS: "127.0.0.1,localhost"
      POSTGRES_DB: acad_assist
      POSTGRES_USER: acad_user
      POSTGRES_PASSWORD: acad_pass
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
